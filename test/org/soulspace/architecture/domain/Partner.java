package org.soulspace.architecture.domain;

import org.soulspace.annotation.domain.Entity;
import org.soulspace.architecture.application.PartnerContactService;

@Entity
public class Partner {

//	PartnerContactService pcs = new PartnerContactService();
	
	void contactPartner(Partner partner) {
//		pcs.makeContact(this, partner);
	}
}
